## 1. Template Layout

- [x] 1.1 Add an app-namespaced base template with shared header, navigation, main content, and footer.
- [x] 1.2 Add templates for the existing home, table list, time-slot list, reservation placeholder, and reservation detail pages.

## 2. Views and Session State

- [x] 2.1 Convert the existing HTML page views to `render()` and pass only the template context they require.
- [x] 2.2 Document the future session login identifier without adding a premature runtime session workflow.

## 3. Documentation and Verification

- [x] 3.1 Document the database/session boundary and template layout in the README and URL API documentation.
- [x] 3.2 Add focused template rendering and session behavior tests.
- [x] 3.3 Run Django checks, migration checks, pytest, Black, Pylint, and OpenSpec validation.
