## 1. HTMX Setup

- [x] 1.1 Add the HTMX dependency, Django configuration, and local static client asset.
- [x] 1.2 Load HTMX and provide CSRF headers from the shared template.

## 2. Availability Interaction

- [x] 2.1 Add widget triggers, an accessible result region, and a server-rendered fragment template.
- [x] 2.2 Add the read-only availability URL and view using the current smallest-table selection rule.

## 3. Verification

- [x] 3.1 Add tests for dynamic availability responses and unchanged normal reservation submission.
- [x] 3.2 Run Django checks, tests, formatting, linting, and OpenSpec validation.
