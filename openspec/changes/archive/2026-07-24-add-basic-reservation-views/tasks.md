## 1. View and URL Implementation

- [x] 1.1 Implement basic function-based reservation views with `HttpResponse`, ORM reads, one ORM write, and one redirect.
- [x] 1.2 Add `reservations/urls.py` and include the app URLs from `config/urls.py`.

## 2. Documentation and Tests

- [x] 2.1 Update README with callable URLs, arguments, return values, and current hard-coded limitations.
- [x] 2.2 Add Django tests for successful pages, redirects, object creation, and 404 behavior.

## 3. Verification

- [x] 3.1 Run Django system checks, pytest, and OpenSpec validation.
