## 1. Schema Implementation

- [x] 1.1 Define `Table`, `TimeSlot`, and `Reservation` models with fields, relationships, ordering, indexes, constraints, and `__str__()` methods.
- [x] 1.2 Register the reservation models in Django admin with useful list displays and filters.

## 2. Database Migration

- [x] 2.1 Generate the initial reservation migration with `manage.py makemigrations reservations`.
- [x] 2.2 Apply migrations with `manage.py migrate`.

## 3. Tests and Documentation

- [x] 3.1 Add focused tests for model labels, ordering, and duplicate-reservation protection.
- [x] 3.2 Update README and OpenSpec project specs to reflect the database design.
- [x] 3.3 Run `manage.py check` and `pytest`.
